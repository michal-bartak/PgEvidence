build/bin
node_modules
frontend/dist
docs/dist
docs/.astro
.DS_Store
**/.DS_Store

# Windows: resource objects generated by `wails build` (embedded into the .exe)
*.syso

# Wails-generated frontend bindings + npm-install cache. Regenerated by every
# `wails build`/`wails dev`, so they only create cross-platform diff noise if
# tracked (line endings / CLI version). The Go source is the source of truth.
frontend/wailsjs/
frontend/package.json.md5
